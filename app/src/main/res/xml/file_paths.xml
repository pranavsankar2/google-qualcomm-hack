<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches context.getExternalFilesDir(null)/scans/ where .reno files are saved -->
    <external-files-path name="scans" path="scans/" />
</paths>
